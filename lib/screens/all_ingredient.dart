import 'package:flutter/material.dart';

class AllIngredient extends StatelessWidget {
  const AllIngredient({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Icon(
        Icons.more_horiz,
        size: 300,
      ),
    );
  }
}